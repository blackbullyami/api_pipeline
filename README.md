# Building an analytics API using FastAPI +time-series Postgres

My Own private Data pipeline!


#Docker
-`docker Build -t api_pipeline -f DockerFile . `
-`docker run API_PIPELINE`
 
#commands becomes
-`docker compose up --watch`
-`docker compose down` or `docker compose down -v`[to remove volumes]"
-`docker run app bin/bash` to go directly into CLI of the container 
